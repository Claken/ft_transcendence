const abortPage = () => {//TODO: afficher qui win et qui loose, sans proposer de reGame
	// context.fillStyle = "white";
	// context.fillText("ABORT", width / 2, height / 2, width);
};

export default abortPage;
